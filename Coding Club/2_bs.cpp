#include<bits/stdc++.h>

using namespace std;

int freq[27];
string s;
int poss(int mid) {

    for(int i = 0; i < mid; ++i) {
        freq[s[i] - 'a']++;
    }

    int n = s.size();

    for(int i = mid; i < sz; ++i) {
        y
    }

    return 0;
}


int main() {
    cin >> s;
    int n = s.size();

    int l = 0, r = (n - 1) / 2, m;

    while(l != r) {
        m = (l + r) / 2;

        if(poss(mid)) {
            high = mid;
        } else {
            low = mid + 1;
        }
    }
    return 0;
}